### docker network 생성
```bash
docker network create docker-network
docker network inspect docker-network
```
### docker mysql 이미지 pull
```bash
docker pull mysql
```
### docker compose
```bash
docker compose up -d  
```